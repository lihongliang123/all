package com.charles.data.structure.linked;

/**
 * Charles Lee original, reprint please indicate the source
 * 链表是以节点的方式进行存储,各个节点不一定是连续存储的。
 * <p>
 * 链表分为单向链表与双向链表,单向链表的节点结构为,包含数据的data域,和指向下一个节点的next域.
 * 双向链表的结构为指向上一个节点的prev域,以及存放数据的data域
 *
 * @author CharlesLee
 */
public interface LinkedList<E>  {
}
